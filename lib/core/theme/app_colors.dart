import 'package:flutter/material.dart';

abstract class AppColors {
  static Color scaffoldBackgroundColor = Color.fromRGBO(20, 20, 20, 1);
  static Color containerColor = Color.fromRGBO(45, 45, 45, 1);
  static Color cursorColor = Colors.grey;
  static Color whiteColor = Colors.white.withOpacity(0.8);
}
